#version 300 es
//Display Pass
#ifdef GL_ES
precision highp float;
#endif //GL_ES

layout (location = 0) out vec4 rtFragColor;

in vec4 vTexcoord;

uniform sampler2D uTex;

void main()
{
	vec2 uv = vTexcoord.xy  * 0.5 + 0.5;
	vec4 color = texture(uTex, uv);

	rtFragColor = color;
}