#version 300 es

#ifdef GL_ES
precision highp float;
#endif

layout (location = 0) out vec4 rtFragColor;

//Light Uniforms
uniform vec4 LightPosition;
uniform vec3 DiffuseReflectivity;
uniform vec3 LightIntensity;

//Varying
in vec4 vColor;
in vec4 vPos;
in vec4 vNormal;
in vec4 vTexcoord;

void main()
{
	//Per-fragment
	//vec3 preColor = LightIntensity * DiffuseReflectivity * max(dot(vColor.xyz, vNormal.xyz), 0.0);
	//rtFragColor = vec4(preColor, 1.0);
	
	//Per-vertex
	rtFragColor = vColor;
}